flask>=3.0
mutagen>=1.45
pillow>=10.0
waitress>=3.0
pywebview>=6.0
requests>=2.31
soundfile>=0.12
numpy>=1.24
scikit-learn>=1.3
python-mpv>=1.0.8
pyloudnorm>=0.1
send2trash>=1.8

# Genius Playlist / Continuous Play sonic embedding (PANNs CNN14, MIT licensed).
# Heavy: adds ~1.1GB to the venv (mostly torch). We do NOT use panns-inference's
# own bootstrap logic (it shells out to `wget`, absent on macOS by default,
# with no error handling) -- see recommend/embedder.py for our own robust
# checkpoint download path.
torch>=2.0
panns-inference>=0.1.1
